import request from 'superagent'

const rootUrl = '/api/v1/users'

// export function addUser(user: string, token: string) {
//   return request
//     .post(`${rootUrl}/fruits`)
//     .set('Authorization', `Bearer ${token}`)
//     .send(user)
//     .then((res) => res.body.user)
//     .catch(logError)
// }
